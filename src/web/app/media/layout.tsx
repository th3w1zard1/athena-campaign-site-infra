export const metadata = {
  title: 'Media Gallery | Athena for Eugene Ward 5 City Council',
  description: 'View photos and media from Athena Aguiar\'s campaign for Eugene Ward 5 City Council. Campaign events, community engagement, and volunteer activities.',
};

export default function MediaLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
} 